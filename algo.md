
1.sum=0, traverse through the string, create checked_string, and update this one by one
2. at each element, an inner loop
3. inner loops checks that the checked_string does contain one a one b one c;
4. if checked_string = true, then update sum+=1
